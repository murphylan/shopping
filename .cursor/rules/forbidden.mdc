---
description: 统一禁止事项清单 - 适用于所有代码文件
alwaysApply: true
version: "1.0"
---

# 禁止事项清单

本文件汇总所有代码规范中的禁止事项，便于统一管理和引用。

## 导入规范

| 禁止项 | 说明 |
|--------|------|
| ❌ 使用相对路径 `../../../` | 使用 `@/` 路径别名 |

## Server Actions

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 返回格式不是 `{ success, data, error }` | 统一返回格式 |
| ❌ 不使用 try-catch | 所有操作必须 try-catch 包裹 |
| ❌ 不检查用户登录状态 | 必须 `await getCurrentUser()` |
| ❌ 使用 Prisma | 项目使用 Drizzle ORM |
| ❌ 页面直接调用 Server Action | 应通过 Hook 封装 |

## Drizzle ORM

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 使用 Prisma | 项目使用 Drizzle ORM |
| ❌ 不使用 `.returning()` | 插入/更新后获取结果 |
| ❌ 忘记 `.limit(1)` | 查询单条记录时必须加 |
| ❌ 查询使用 `select *` | 只选择需要的字段 |
| ❌ 大数据量不设置查询上限 | 使用 `limit(5000)` 保护 |
| ❌ 不为常用过滤字段添加索引 | 添加必要的数据库索引 |

## React Query

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 自定义 mutation 对象 | 使用 `useMutation` |
| ❌ 不使用 `useMutation` | 变更操作必须使用 |
| ❌ 手动管理 isLoading/error | 使用 mutation 内置状态 |

## React 组件

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 使用 `window.confirm` | 使用 `DeleteConfirm` 组件 |
| ❌ 使用 `isLoading` | 使用 `isPending` |
| ❌ 将 `start`/`done` 添加到 useCallback 依赖 | 会导致死循环 |
| ❌ 表格列定义不使用 `useMemo` | 会导致无限渲染 |
| ❌ 在 `cell` 中直接定义复杂组件 | 应提取为独立组件 |

## Zustand Store

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 在 Store 中存储服务端数据 | 使用 React Query |
| ❌ 忘记添加 `"use client"` | 必须添加指令 |
| ❌ 在 Store 中发起网络请求 | 在 Hook 中处理 |

## 表格组件

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 忘记设置 `size` 属性 | 会导致列宽不稳定 |
| ❌ 筛选列不配置 `filterFn` | 多选筛选会失效 |
| ❌ 大数据量使用标准 DataTable | 使用 DataTableVirtualized |
| ❌ 虚拟表格启用 URL 同步 | 会导致 URL 过长 |
| ❌ 硬编码表格高度 | 使用 Flex 自动填充 |
| ❌ 忘记 `sticky top-0` 固定表头 | 滚动时表头应固定 |
| ❌ 在展开行中嵌套过深的表格 | 保持结构简洁 |
| ❌ 排序状态放在 `useMemo` 依赖外 | 确保响应式更新 |
| ❌ 在 `filteredData` 中直接修改原数组 | 使用 `[...array]` 拷贝 |

## UI 样式

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 使用全大写文字 | 除非是缩写如 ID、URL |
| ❌ 表头居中对齐 | 应左对齐 |
| ❌ 使用非 8px 网格的间距 | 遵循 8px 网格系统 |
| ❌ 按钮不带图标 | 图标 + 文字组合 |
| ❌ 硬编码颜色值（如 `#333`） | 使用 Tailwind 颜色类 |
| ❌ 硬编码高度（如 `h-[500px]`） | 使用 Flex 填充 |
| ❌ 表单字段不带 Label | 必须有标签 |
| ❌ 状态不使用语义化颜色 | 绿色成功、红色失败 |
| ❌ 使用 `grid-cols-2` 在抽屉中 | 抽屉宽度有限 |
| ❌ 操作列平铺多个按钮 | 使用 DropdownMenu |

## Excel 导出

| 禁止项 | 正确做法 |
|--------|----------|
| ❌ 直接导出不弹对话框 | 先弹出对话框让用户确认 |
| ❌ 硬编码文件名 | 包含时间戳 |
| ❌ 忘记设置列宽 | 否则内容可能被截断 |
| ❌ 忘记 toast 提示导出结果 | 必须提示成功/失败 |
