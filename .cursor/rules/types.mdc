---
description: 类型定义规范 - 适用于类型文件
globs:
  - types/**/*.ts
version: "1.0"
dependencies: []
---

# 类型定义规范

**位置**: `types/{domain}-types.ts`

## 模板

```typescript
import z from "zod";

// 1. 实体接口（数据库返回的完整类型）
export interface Entity {
  id: string;
  businessId: string;
  title: string;
  description: string | null;
  status: string;
  createdBy: string;
  createdAt: Date;
  updatedAt: Date;
}

// 2. Zod Schema（表单验证）
export const entitySchema = z.object({
  id: z.string().optional(),
  businessId: z.string().optional(),
  title: z.string().min(1, "标题不能为空"),
  description: z.string().optional().nullable(),
  status: z.string().optional(),
});

// 3. 表单数据类型（从 Schema 推断）
export type EntityFormData = z.infer<typeof entitySchema>;

// 4. 列表查询参数（可选）
export interface EntityFilters {
  status?: string;
  search?: string;
  page?: number;
  pageSize?: number;
}
```

## 命名约定

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 实体接口 | `{Entity}` | `Requirement`, `Module` |
| 表单 Schema | `{entity}Schema` | `requirementSchema` |
| 表单数据 | `{Entity}FormData` | `RequirementFormData` |
| 过滤参数 | `{Entity}Filters` | `RequirementFilters` |
| 列表响应 | `{Entity}ListResponse` | `RequirementListResponse` |

## 常用 Zod 验证

```typescript
// 必填字符串
z.string().min(1, "不能为空")

// 可选字符串
z.string().optional()

// 可为 null
z.string().nullable()

// 可选且可为 null
z.string().optional().nullable()

// 枚举
z.enum(["draft", "published", "archived"])

// 数字
z.number().min(0).max(100)

// 日期
z.date()
z.string().datetime() // ISO 字符串

// 数组
z.array(z.string())

// 对象
z.object({ ... })
```
