---
description: 核心代码规范 - 适用于所有代码文件
alwaysApply: true
version: "1.0"
dependencies: []
---

# Murphy Requirement 核心代码规范

## 技术栈

- **框架**: Next.js 16 (App Router) + React 19 + TypeScript
- **数据库**: Drizzle ORM + PostgreSQL（禁止使用 Prisma）
- **状态管理**: TanStack React Query (服务端) + Zustand (客户端) + nuqs (URL)
- **UI**: Radix UI + Tailwind CSS + shadcn/ui
- **表单**: React Hook Form + Zod
- **提示**: sonner (toast) + nextjs-toploader (加载条)

## 项目结构

```
app/(platform)/          # 平台页面（需登录）
components/              # 共享组件
  ui/                    # shadcn/ui 基础组件
  data-table/            # 表格组件
  shared/                # 业务共享组件
hooks/                   # 自定义 Hooks (use-{domain}.ts)
server/
  actions/               # Server Actions ({entity}Action.ts)
  db/schema/             # Drizzle Schema
types/                   # 类型定义 ({domain}-types.ts)
lib/                     # 工具函数
```

## 强制规则

### 路径别名

- ✅ 使用 `@/` 路径别名
- ❌ 禁止使用相对路径 `../../../`

### 导入顺序

1. React
2. Next.js
3. Third-party (sonner, nextjs-toploader, @tanstack/react-query)
4. UI Components (@/components/ui/*)
5. Business Components
6. Hooks (@/hooks/*)
7. Server Actions (@/server/actions/*)
8. Types (import type from @/types/*)

### 组件结构

1. Hooks (useRouter, useTopLoader, 自定义 hooks)
2. State (useState)
3. Memoized values (useMemo)
4. Callbacks (useCallback)
5. Effects (useEffect)
6. Render (return)

## 相关规范文件

各领域详细规范请参考以下文件：

| 领域 | 规范文件 | 说明 |
|------|----------|------|
| 禁止事项 | `forbidden.mdc` | 统一禁止事项清单 |
| Server Actions | `server-actions.mdc` | 服务端操作规范 |
| React Query | `react-query.mdc` | 数据 Hook 规范 |
| Zustand | `zustand.mdc` | 客户端状态管理 |
| Drizzle ORM | `drizzle.mdc` | 数据库操作规范 |
| 数据架构 | `data-architecture.mdc` | 数据调用链路 |
| UI 样式 | `ui-style.mdc` | Databricks 风格样式 |
| 组件模式 | `ui-components.mdc` | React 组件与 UI 模式 |
| 表格组件 | `data-table.mdc` | DataTable + 自定义表格 |
| 类型定义 | `types.mdc` | 类型定义规范 |
| Excel 导出 | `excel-export.mdc` | 导出功能规范 |

## 禁止事项

> 完整禁止事项清单请参考 `forbidden.mdc`

核心禁止事项摘要：

- ❌ 使用相对路径 `../../../`（使用 `@/` 别名）
- ❌ 使用 Prisma（使用 Drizzle ORM）
- ❌ 使用 `isLoading`（使用 `isPending`）
- ❌ 使用 `window.confirm`（使用 DeleteConfirm 组件）
- ❌ 硬编码高度（使用 Flex 填充）
- ❌ 按钮不带图标

## 对话规范

每次对话完成后，以 "恭喜发财 🧧" 结尾。
